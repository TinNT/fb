<h1>Listing tabs</h1>

<table>
  <tr>
    <th>Fb page</th>
    <th>Care url</th>
  </tr>

<% @tabs.each do |tab| %>
  <tr>
    <td><%=h tab.fb_page_id %></td>
    <td><%=h tab.care_url %></td>
    <td><%= link_to 'Show', tab %></td>
    <td><%= link_to 'Edit', edit_tab_path(tab) %></td>
    <td><%= link_to 'Destroy', tab, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New tab', new_tab_path %>